apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  sample-api-overview.json: |-
    {{- .Files.Get "dashboards/sample-api-overview.json" | nindent 4 }}
