apiVersion: v1
kind: Service
metadata:
  name: sample-api
  namespace: applications
  labels:
    app: sample-api
spec:
  type: ClusterIP
  selector:
    app: sample-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
