apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-sre-reports
  namespace: ai-sre
  labels:
    app: ai-sre-agent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
